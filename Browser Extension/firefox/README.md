# Fast Native Download Manager Firefox Extension

Placeholder folder for the Firefox WebExtension build.

The Chrome implementation is in `../chrome` and currently uses the custom macOS URL scheme:

`fastndm://download?url=...`

